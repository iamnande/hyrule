apiVersion: v1
kind: Service
metadata:
  name: {{ include "pings.fullname" . }}
  labels:
    {{- include "pings.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "pings.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
