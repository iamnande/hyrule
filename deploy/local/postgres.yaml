apiVersion: apps/v1
kind: Deployment
metadata:
  name: hyrule-database
  labels:
    app.kubernetes.io/name: hyrule-database
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: hyrule-database
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hyrule-database
    spec:
      containers:
        - name: postgres
          image: docker.io/postgres:17.2-alpine
          env:
            - name: POSTGRES_USER
              value: hyrule_owner
            - name: POSTGRES_PASSWORD
              value: hyrule_owner
            - name: POSTGRES_DB
              value: hyrule
          ports:
            - name: postgres
              containerPort: 5432
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "hyrule_owner", "-d", "hyrule"]
            initialDelaySeconds: 2
            periodSeconds: 2
          volumeMounts:
            - name: init
              mountPath: /docker-entrypoint-initdb.d
              readOnly: true
      volumes:
        - name: init
          configMap:
            name: hyrule-database-init
---
apiVersion: v1
kind: Service
metadata:
  name: hyrule-database
spec:
  selector:
    app.kubernetes.io/name: hyrule-database
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
